.footer {
  padding: 30rpx 0rpx;
  text-align: center;
  background: #f0eeee;
  border-top: 1px solid var(--subColor);
  color: var(--greyColor);
  line-height: 1.3em;
}

.footer .row {
  font-size: 26rpx;
}
